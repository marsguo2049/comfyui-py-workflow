# Offline Story Studio v0.5

Offline Story Studio 是 `comfyui-py-workflow` 的纯本地操作页面。它把故事文档、单张视觉参考图、LM Studio 分镜、ComfyUI 关键帧与视频生成，以及最终结果预览放在同一个本机页面中。

正式创作时，页面和后台只接受 `127.0.0.1`、`localhost` 或 `::1` 地址。它不提供云端 LLM、云生图、在线素材或遥测功能。

## 断网前准备

在项目目录中安装全部本地依赖：

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[all]"
```

确认下面的内容已经存在：

- LM Studio 和至少一个已下载的本地文字模型。
- ComfyUI、三个 API 工作流需要的模型与自定义节点。
- `pypdf` 和 `av`。
- 扫描 PDF 所需的 RapidOCR、ONNX Runtime、PyMuPDF，以及 RapidOCR 自带的三个 ONNX 小模型。

页面的“本机服务检查”会显示这些能力。缺失内容必须在断网前补齐；正式运行时不会自动下载。

## 启动

双击项目根目录下的：

```text
start-local-ui.bat
```

页面默认打开：

```text
http://127.0.0.1:7860
```

也可以从终端启动：

```powershell
.\.venv\Scripts\cpw-local-ui.exe
```

页面只绑定本机回环地址。若指定公网、局域网或云端服务地址，离线预检会直接阻止。

## 推荐操作顺序

1. 启动 LM Studio，加载本地文字模型，并开启 Local Server。
2. 在页面上传 PDF、DOCX、Markdown、TXT，或者直接输入故事；可以同时选择一张参考图。
3. 点击“开始本地分析”。
4. 从精简版、推荐版、完整版中选择时长，也可以填写 15-180 秒之间的 5 秒倍数。
5. 点击“确认时长，生成详细分镜”。
6. 检查并修改 `story-plan.json`。保存时会校验镜头数量、时长、转场和所有提示词字段。
7. 页面尝试自动卸载 LM Studio 模型。若无法卸载，会明确提示手动卸载。
8. 启动 ComfyUI，点击“重新检测”。预检会检查 API 工作流、节点类和工作流引用的模型文件。
9. 点击“第二次确认：开始生成”。
10. 在页面查看关键帧、视频片段、完成秒数、镜头进度和最终视频。

LM Studio 与 ComfyUI 的窗口可以同时存在，但显存有限时不要同时加载文字大模型和 H3 视频模型。

## 单张参考图

页面支持为一个故事项目添加一张 PNG、JPG/JPEG 或 WebP 图片。参考图最多 25 MB，只复制到当前本地项目，不会送到 LM Studio 或任何云端服务。

- 没有参考图时，每个独立新场景仍由 Z-Image 根据文字生成起始帧。
- 有参考图时，每个独立新场景改由 Qwen Image Edit 读取同一张参考图，按照分镜重构画面，并要求保留图中可见的人物身份、面部、服装、重要物件或画风。
- 连续镜头不重新读取原图，而是直接继承上一镜的结束帧，避免破坏时间连续性。
- 每一镜的结束帧仍由 Qwen Image Edit 在起始帧上做受控变化，之后交给 H3 生成首尾帧视频。

LM Studio 只会知道“项目存在一张视觉参考图”，用于写出适合图片编辑的分镜指令；第一版不会把图片本身加载进文字模型，也不会自动识别人名或图片内容。请在故事或补充画风中说明参考图扮演的角色，例如“参考图中的女性是女主角”。

在渲染开始前可以替换图片。一旦 `render/run.json` 已经建立，系统会阻止更换，以免断点继续时混入另一张图片。图片内容的 SHA-256 会写入本地运行记录，用来校验续跑一致性。

## 时长推荐

本地文字模型首先提取故事摘要、关键事件、类型和视觉复杂度，再给出一个推荐时长。程序根据推荐值计算三个选项，并按每段 5 秒取整。

当前渲染时间估算使用本机实测基准：每个 5 秒 H3 片段约需 5.2 分钟。实际速度会随模型加载、画面复杂度和显存状态变化。

第一版单个视频限制为 15-180 秒。更长的文档应先拆成多集，避免一次任务持续数小时。

## PDF 与离线 OCR

- 有文字层的 PDF 优先使用 `pypdf` 直接读取。
- 没有文字层，或者文字层含有大量控制符、乱码和异常符号时，程序使用 PyMuPDF 在内存中渲染页面，再交给 RapidOCR 和 ONNX Runtime。
- 默认中文/英文 OCR 模型保存在当前 `.venv` 中，不需要运行时下载。
- OCR 可能出现错字。生成时长建议前应检查页面显示的摘要是否准确。
- 加密、破损或画质过低的 PDF 会明确报错。

## LM Studio Structured Response

故事分析和分镜使用 JSON Schema 约束输出。程序会为这些结构化任务发送
`reasoning_effort: none`，避免推理模型把全部输出额度用在
`reasoning_content`，最后留下空的 JSON 正文。

如果生成仍因 `finish_reason=length` 失败，页面会保留详细错误。先使用“重新分析文档”
重试；较长视频可在 LM Studio 中把模型上下文从 8192 提高到 16384 或更高，再重新加载
模型。30 秒六镜头已经在 8192 上下文中通过本地验证，更长分镜需要的上下文会增加。

## 进度与继续生成

每个完整镜头完成后都会更新：

```text
outputs/offline-studio/<项目编号>/render/run.json
```

若任务失败或在模型步骤之间停止，再次点击“继续未完成的生成”会验证现有文件并跳过已经完成的片段。正在运行的单个 ComfyUI 模型步骤不会被强制杀死；停止请求会在当前步骤结束后生效。

已经成功完成的任务不会重复渲染。

## MiniMax H3 台词与音频

生成分镜前可以选择三种台词策略：

- “由模型判断”：只在叙事确实需要时使用短台词；没有精确台词标签的镜头会禁止自行说话。
- “不要台词或旁白”：允许环境声、动作音效和配乐，但禁止台词、旁白、耳语与歌唱。
- “需要台词或旁白”：至少一个镜头必须包含可验证的精确台词。

H3 提示词使用官方 FL2VA 结构：首尾帧时间对齐说明、integrated_multimodal_description、
overall_soundscape 和 non_diegetic_music。台词写成
(S1) says: <d>[Chinese] 精确台词。</d>；只有 <d> 标签中的内容可以作为可说出口的文字。
每个五秒片段最多安排一句简短完整的台词，避免模型在剩余时间自行补充含混语音。

最终 MP4 使用 fast-start 封装，并支持浏览器的普通、开放式和尾部字节 Range 请求，因此可以边读边播和拖动进度。

## 本地输出

```text
outputs/offline-studio/<项目编号>/
├─ source/                  原始故事文件
│  └─ reference/            可选的单张本地参考图
├─ project.json            页面和任务状态
├─ story-analysis.json     本地故事分析
├─ duration-options.json   时长选项与耗时估算
├─ story-plan.json         最终分镜与模型提示词
└─ render/
   ├─ frame-*.png          关键帧
   ├─ clip-*-raw.mp4       每段原始视频
   ├─ run.json             生成记录与断点状态
   └─ final.mp4            最终视频
```

整个 `outputs/` 目录已被 Git 忽略。页面不会执行 Git 提交或推送。

## 断网验收建议

1. 先在联网状态运行完整测试：

   ```powershell
   .\.venv\Scripts\python.exe -m pytest -q
   ```

2. 断开网络。
3. 启动 LM Studio、加载本地模型并开启 Local Server。
4. 双击 `start-local-ui.bat`。
5. 上传一份 PDF，完成故事分析和分镜。
6. 卸载 LM Studio 模型，启动 ComfyUI。
7. 生成一个较短测试视频并检查最终路径。

建议第一次离线测试选择 15 或 30 秒，确认完整链路后再尝试更长视频。
