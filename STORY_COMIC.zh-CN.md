# 故事漫画 · Offline Studio

把故事变成连续图片，并配上可编辑的旁白与对白。它复用故事视频已有的 LM Studio、Z-Image Turbo 和 Qwen Image Edit 2509 工作流，使用独立的漫画分镜提示词，不经过视频生成阶段。

[查看公开界面预览](https://marsguo2049.github.io/comfyui-py-workflow/#comic)。公开页只展示虚构分镜和仓库已有的单车样图排版，不连接本机服务、不读取私人历史。

## 开始创作

1. 安装项目依赖，启动 LM Studio 本地服务，加载支持结构化输出的文字模型。ComfyUI 需准备 Z-Image 与 Qwen 工作流的模型和节点，见 [工作流说明](workflows/README.md)；漫画不需要 H3 视频模型或视频编码器。
2. 双击 `start-local-ui.bat`，选择“故事漫画”。也可以运行 `cpw-local-ui --view comic`。服务地址在统一的“服务设置”中填写，仅接受本机回环地址。
3. 输入故事，或选择 TXT、Markdown、DOCX、PDF 文档；选择文档时以文档为准。直接输入最多 120,000 字符，文档最多 100 MB；PDF 读取与 OCR 沿用 [离线工作台的依赖和限制](OFFLINE_STUDIO.zh-CN.md#pdf-与离线-ocr)。
4. 可选上传一张 PNG、JPEG 或 WebP 参考图，最多 25 MB。在故事中说明图中人物的身份和外观；文字模型不直接识别参考图。
5. 选择 2–16 格、画幅与画风，点击“生成漫画分镜”。长文档先摘要；默认在分镜完成后尝试卸载文字模型，失败时会提示手动卸载。
6. 审阅标题、人物设定和每一格的画面提示词、旁白、对白、参考方式。修改后保存，再点击“确认分镜，生成漫画”。“这一格发生什么”用于理解分镜；要改变实际画面，请修改对应的“画面提示词”。
7. 每完成一格，阅读区自动更新。全部完成后可打开或下载阅读页，以及下载 ZIP 图片包。

参考图会按所选画幅居中裁切并缩放，再交给 Qwen。重要人物请留在画面中心，或提前裁好参考图。常用画幅包含 1:1、3:4、4:3、16:9、9:16，分辨率按现有工作流约 0.6 MP 的规则计算。

## 连续画面与重做

- 无参考图时，首格由 Z-Image 根据文字生成；后续由 Qwen 编辑参考画面。
- 有参考图时，首格也由 Qwen 生成。“人物参考图 / 首格”始终使用上传图；没有上传图时使用已生成的首格，适合切换场景。
- “上一格”使用前一格生成结果，适合连续动作。人物设定与统一画风会注入每一格提示词，但模型仍可能出现人物、服装或物件漂移，需要审阅和重做。
- “从此格重做”保留前面的图片，重新生成这一格及后续各格，避免后续引用旧画面。改变画面提示词或参考方式也会使受影响的后续图片失效；修改全局画风、人物设定或负向提示词会使全部图片失效。
- 重做前可先修改画面提示词；相同提示词和种子重复运行，可能得到相同画面。需要全新随机结果时，可修改基础种子并从第 1 格重做。
- 只修改标题、旁白、对白或分镜说明会保留已有图片；若已全部完成，保存时直接更新阅读页和 ZIP，无需再次生图。
- “停止”在当前模型请求完成后生效，不会中断 ComfyUI 中其他任务。已完成图片保留；失败或服务重启后可从历史任务打开并继续。续跑需使用原种子，若要改变种子，请从第 1 格重做。

漫画工具同一时间只运行一个规划或生成任务。故事视频、批量工具与漫画的任务控制独立；显存有限时请逐项运行，避免同时提交多个工作区任务。

## 阅读与导出

旁白和对白以清晰文字排在每格图片下方，不要求图片模型绘制中文。阅读页适配窄屏；桌面双列、窄屏单列。第一版不包含自动气泡定位、自由拖拽分页或原生 PDF 导出。

- `comic.html`：包含内嵌图片的独立阅读页，断网也能打开。可通过浏览器“打印 → 保存为 PDF”输出，实际分页取决于浏览器和纸张设置。
- `comic.zip`：包含阅读页、逐格 PNG 和 `comic-plan.json`。不包含输入故事文档、原始参考图或任务历史；分镜本身包含你审阅后的故事画面与文字，分享前请自行检查。
- 每格图片也可单独查看、下载。

## 本地存储

漫画任务保存在 `outputs/offline-studio/comic-jobs/<任务编号>/`，包含输入、参考图、分镜、进度和生成结果；`outputs/` 已被 Git 忽略。关闭浏览器不会停止后台任务；关闭服务后，可在重启后从最近任务继续。修改故事或参考图请新建漫画。

与其他工作区一样，默认仅在本机运行，不开放远程接口。GitHub Pages 展示页独立于本地后台，禁止网络连接，不会展示这些目录中的内容。若自定义 `--project-root`，请把目标目录放在仓库之外或加入 `.gitignore`。
