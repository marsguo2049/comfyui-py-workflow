# 批量首尾帧视频

启动 `start-local-ui.bat`，在独立的 **ComfyUI Workbench → 批量首尾帧视频** 中操作。Workbench 默认使用 `http://127.0.0.1:7860/#batch`，只连接 ComfyUI；故事、漫画与翻译位于 [Offline Studio](https://github.com/marsguo2049/offline-studio)（默认端口 `7870`）。

## 使用

1. 在首帧、尾帧区域分别选择图片或文件夹。支持多选；文件夹会包含子文件夹中的图片，跳过非图片文件。
2. 选择配对方式，检查配对预览。
3. 填写所有视频共用的提示词，设置每段秒数、画面比例、分辨率及采样步数。
4. 点击开始。每完成一段即可播放、下载，或打开任务文件夹。

只需要运行本机 ComfyUI，并具备仓库内 MiniMax H3 首尾帧工作流所需的模型与节点；无需 LM Studio。模型和节点说明见 [工作流文档](workflows/README.md)。

## 配对规则

| 首帧 | 尾帧 | 结果 |
|---|---|---|
| 一张 | 一张 | 生成一段 |
| 一张 | 多张 | 首帧复用，每张尾帧生成一段 |
| 多张 | 一张 | 尾帧复用，每张首帧生成一段 |
| 多张 | 多张 | 按所选规则一一配对 |

自然排序按文件名排列，数字按数值比较，例如 `2.png` 排在 `10.png` 前。两侧多图时数量必须一致。

同名匹配忽略扩展名和大小写，例如 `A.png` 对应 `a.jpg`；不按子文件夹名区分。两侧多图时，任一侧出现相同主文件名，或者两侧名称未完全匹配，都会阻止启动。只有一侧单图时优先复用单图，无需文件名一致。

合计最多 200 张图片、500 MB，单张不超过 25 MB。支持 PNG、JPEG、WebP、GIF、BMP、TIFF；视频首尾画面取决于 ComfyUI 对输入图片的解码。

## 参数与时长

- 时长：每段 1–30 秒，默认 5 秒。这是工作台的输入范围，并非模型效果或显存需求的保证。
- 固定 24 fps，帧数按内置 H3 工作流约束 `17n + 5` 对齐。例如选择 5 秒时生成 124 帧，预计约 5.17 秒。保留完整生成视频，不通过裁掉尾帧强行凑整秒。
- 比例：方形 1:1、横屏 16:9、竖屏 9:16。两侧图片按目标比例居中裁切。
- 分辨率：0.1–2 百万像素，默认 0.6；宽高对齐到 32 的倍数。
- 采样步数：默认 4，对应内置加速 LoRA；可以调整至 1–100。
- 高级参数：基础种子、每段种子递增、单项等待超时和自定义 API 工作流路径。默认视频超时为 1800 秒。
- 内置视频工作流不提供独立负向提示词；该字段仅用于批量图片编辑。

自定义工作流须保留内置 H3 的节点编号、类型及连接；工作台会替换首尾帧、提示词、种子、时长、比例、分辨率、步数及输出名称。帧率及帧数表达式统一使用内置配置。

## 结果和异常

任务默认保存在 `outputs/comfyui-workbench/batch-jobs/<任务编号>/`：输入图位于 `input/`，MP4 位于 `output/clip-0001.mp4` 等文件，`job.json` 保存参数、配对、ComfyUI 任务编号和结果。旧版 `outputs/offline-studio/` 历史不会删除或自动迁移。

停止按钮会等待当前视频结束，不再提交下一段。已确认的单段生成失败会记录原因并继续其他配对。等待超时或提交后连接异常时，远端任务可能仍在运行，因此停止后续提交，显示待确认提示及已知任务编号；请在 ComfyUI 队列与输出目录检查当前任务。

关闭网页不会终止后台任务。重启服务后，未完成任务标为已中断，保留已有结果。暂不自动恢复执行；可新建任务处理剩余配对。
